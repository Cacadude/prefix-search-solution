# Подготовка проекта к отправке

## Чек-лист перед отправкой

### ✅ Проверка работоспособности

1. **Запуск через Docker Compose**:
   ```bash
   cd C:\Users\arsgo\prefix-search-solution
   docker compose down -v  # Очистка старых данных
   docker compose up -d    # Запуск
   ```

2. **Проверка здоровья сервисов** (подождите 30-60 секунд):
   ```bash
   curl http://localhost:5000/health
   # Ожидается: {"status":"ok","elasticsearch":"connected"}
   ```

3. **Тестовый запрос**:
   ```bash
   curl "http://localhost:5000/search?query=масло&top_k=3"
   ```

4. **Запуск оценки качества**:
   ```bash
   python tools/evaluate.py --queries data/prefix_queries.csv --output reports/evaluation_results.csv --base-url http://localhost:5000
   ```

5. **Проверка метрик**:
   ```bash
   cat reports/metrics.json
   # Должно быть: coverage_percent >= 70%
   ```

### ✅ Проверка файлов

Убедитесь, что все необходимые файлы присутствуют:

```
prefix-search-solution/
├── src/
│   ├── load_catalog.py          ✅
│   └── search_api.py            ✅
├── tools/
│   └── evaluate.py              ✅
├── data/
│   ├── catalog_products.xml     ✅
│   └── prefix_queries.csv       ✅
├── docker-compose.yml           ✅
├── Dockerfile                   ✅
├── requirements.txt             ✅
├── README.md                    ✅
├── QUICKSTART.md                ✅
├── ARCHITECTURE.md              ✅
├── SOLUTION_SUMMARY.md          ✅
├── TESTING.md                   ✅
├── CHANGELOG.md                 ✅
├── PROJECT_STRUCTURE.md         ✅
└── .gitignore                   ✅
```

### ✅ Очистка перед отправкой

**Удалите временные файлы и данные**:

```bash
# Остановка контейнеров
docker compose down -v

# Удаление временных файлов Python
Remove-Item -Recurse -Force __pycache__ -ErrorAction SilentlyContinue
Get-ChildItem -Recurse -Filter "*.pyc" | Remove-Item -Force -ErrorAction SilentlyContinue

# Удаление старых отчетов (опционально, можно оставить для примера)
# Remove-Item reports\*.csv -ErrorAction SilentlyContinue
# Remove-Item reports\*.json -ErrorAction SilentlyContinue
```

## Варианты отправки

### Вариант 1: Git репозиторий (рекомендуется)

#### 1.1. Инициализация репозитория

```bash
cd C:\Users\arsgo\prefix-search-solution

# Инициализация Git (если еще не инициализирован)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Prefix search solution with 96.67% coverage"
```

#### 1.2. Создание репозитория на GitHub/GitLab

1. Создайте новый репозиторий на GitHub/GitLab
2. **НЕ** инициализируйте его с README, .gitignore или лицензией
3. Скопируйте URL репозитория

#### 1.3. Подключение и отправка

```bash
# Добавление remote (замените URL на ваш)
git remote add origin https://github.com/ваш-username/prefix-search-solution.git

# Отправка кода
git branch -M main
git push -u origin main
```

#### 1.4. Предоставление доступа

- **GitHub**: Settings → Collaborators → Add people
- **GitLab**: Project → Members → Invite members
- **Email**: artem.kruglov@diginetica.com

### Вариант 2: Архив (ZIP)

#### 2.1. Создание архива через PowerShell

```powershell
cd C:\Users\arsgo\prefix-search-solution

# Создание архива (исключая ненужные файлы)
$exclude = @('__pycache__', '*.pyc', '.git', 'es_data', '*.log')
Compress-Archive -Path * -DestinationPath ..\prefix-search-solution.zip -Force
```

#### 2.2. Или через проводник Windows

1. Откройте папку `C:\Users\arsgo\prefix-search-solution`
2. Выделите все файлы и папки (Ctrl+A)
3. Правый клик → Отправить → Сжатая ZIP-папка
4. Переименуйте архив в `prefix-search-solution.zip`

#### 2.3. Проверка размера архива

```bash
# Размер должен быть примерно 1-5 MB
Get-Item ..\prefix-search-solution.zip | Select-Object Name, @{Name="SizeMB";Expression={[math]::Round($_.Length/1MB,2)}}
```

#### 2.4. Отправка архива

- **Email**: прикрепите архив к письму на artem.kruglov@diginetica.com
- **Облачное хранилище**: загрузите на Google Drive / Dropbox / Яндекс.Диск и отправьте ссылку
- **Файлообменник**: загрузите на файлообменник и отправьте ссылку

### Вариант 3: Прямая отправка файлов

Если репозиторий или архив не подходят, можно отправить файлы напрямую:

1. Скопируйте всю папку `prefix-search-solution` в удобное место
2. Убедитесь, что все файлы на месте
3. Отправьте через удобный вам способ

## Что НЕ нужно отправлять

❌ **Исключите из отправки**:

- `__pycache__/` - кэш Python
- `*.pyc` - скомпилированные файлы Python
- `.git/` - папка Git (если отправляете архив)
- `es_data/` - данные Elasticsearch (будут созданы при запуске)
- `*.log` - логи
- `.env` - переменные окружения (если есть)
- `.vscode/`, `.idea/` - настройки IDE

**Все это уже исключено в `.gitignore`**, но проверьте перед созданием архива.

## Финальная проверка перед отправкой

### ✅ Чек-лист

- [ ] Все сервисы запускаются через `docker compose up -d`
- [ ] API отвечает на запросы (`/health` и `/search`)
- [ ] Оценка качества проходит успешно
- [ ] Покрытие ≥70% (у вас 96.67% ✅)
- [ ] Все файлы документации на месте
- [ ] README.md содержит актуальную информацию
- [ ] Нет временных файлов (__pycache__, *.pyc)
- [ ] Размер архива разумный (1-5 MB)

### ✅ Тестовая проверка на чистой системе

Если есть возможность, протестируйте на другой машине:

1. Скопируйте проект
2. Запустите `docker compose up -d`
3. Проверьте работу API
4. Запустите оценку качества

## Информация для сопроводительного письма

При отправке укажите:

```
Тема: Тестовое задание - Префиксный поиск

Здравствуйте!

Отправляю решение тестового задания по префиксному поиску.

Результаты:
- Покрытие: 96.67% (58 из 60 запросов)
- Средняя задержка: 67.51 мс
- Все требования выполнены

Инструкции по запуску:
1. docker compose up -d
2. Подождать 30-60 секунд
3. curl http://localhost:5000/health
4. python tools/evaluate.py --queries data/prefix_queries.csv --output reports/evaluation_results.csv --base-url http://localhost:5000

Подробная документация в README.md и QUICKSTART.md.

С уважением,
[Ваше имя]
```

## Быстрая команда для подготовки

```powershell
# Полная подготовка к отправке (PowerShell)
cd C:\Users\arsgo\prefix-search-solution

# Остановка контейнеров
docker compose down -v

# Очистка временных файлов
Get-ChildItem -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Recurse -Filter "*.pyc" | Remove-Item -Force -ErrorAction SilentlyContinue

# Создание архива
$date = Get-Date -Format "yyyy-MM-dd"
Compress-Archive -Path * -DestinationPath "..\prefix-search-solution-$date.zip" -Force

Write-Host "Архив создан: ..\prefix-search-solution-$date.zip" -ForegroundColor Green
```

## Контакты для отправки

- **Email**: artem.kruglov@diginetica.com
- **Формат**: ZIP архив или ссылка на Git репозиторий

---

**Удачи с отправкой! 🚀**

